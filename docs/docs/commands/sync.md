---
sidebar_position: 3
---

# Sync

The **Sync** command immediately refreshes Todoist metadata and every mounted Query block, then synchronizes every enabled Project sync mapping.

Use **Tasks Bridge: Sync** whenever you want both modes to refresh without waiting for Auto-refresh. The command remains available when Auto-refresh is disabled. If Project sync is disabled or has no enabled mappings, Query blocks still refresh normally.
